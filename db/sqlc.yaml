version: "2"
sql:
  - engine: postgresql
    schema:
      - "migrations"
    queries:
      - "queries"
    gen:
      go:
        package: "sqlc"
        out: "../internal/store/sqlc"
        sql_package: "database/sql"
        emit_empty_slices: true
        emit_interface: false
